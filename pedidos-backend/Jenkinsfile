pipeline {
    agent {
        docker {
            image 'alpine/helm:3.15.3' // imagen oficial de Helm
            args '-u root:root'        // opcional si necesitas permisos
        }
    }
    environment {
        GIT_COMMIT_SHORT = GIT_COMMIT.take(7)
    }

    triggers {
        githubPush()
    }
    stages {
        stage('Helm version') {
            steps {
                sh 'helm version'
            }
        }
        stage('Checkout') {
            steps {
                git url: 'https://github.com/alejandrazuleta1/k8s', branch: 'main'
            }
        }
        stage('Build Docker Image') {
            steps {
                dir('pedidos-backend') {
                    script {
                        sh 'docker build -t alezuleta/my-tech:${GIT_COMMIT_SHORT} .'
                    }
                }
            }
        }
        stage('Push to Registry') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerhub-credentials',
                                             usernameVariable: 'USER',
                                             passwordVariable: 'PASS')]) {
                    sh 'docker login -u $USER -p $PASS'
                    sh 'docker push alezuleta/my-tech:${GIT_COMMIT_SHORT}'
                }
            }
        }
        stage('Update Helm Values') {
            steps {
                script {
                    sh """
                        cd charts
                        sed -i 's/tag: .*/tag: ${IMAGE_TAG}/' pedido-app/values.yaml
                        helm package ./
                        ls -l
                    """
                }
            }
        }
    }
}